name: Keep Alive

on:
  schedule:
    # Runs every Sunday at 12:00 UTC — prevents GitHub from disabling scheduled workflows
    - cron: "0 12 * * 0"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Keep-alive ping — workflows stay active."
